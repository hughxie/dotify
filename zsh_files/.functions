#!/bin/bash

# make a directory then cd into it
function mkd() {
	mkdir -p "$@" && cd "$_";
}

# Visualize traceroute
function traceroute-maper {
  open "https://stefansundin.github.io/traceroute-maper/?trace=$(traceroute -q1 $*)"
}
